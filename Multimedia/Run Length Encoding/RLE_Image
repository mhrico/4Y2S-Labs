import cv2
import matplotlib.pyplot as plt

img = cv2.imread('./image.jpg', 0)

planes = []
for i in range(8):
    planes.append(img & 2 ** i)
    plt.subplot(2, 4, i+1)
    plt.imshow(planes[i], 'gray')
plt.show()
